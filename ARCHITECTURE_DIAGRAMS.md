# System Architecture & Data Flow Diagrams

## 🏗️ Application Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    HTTP Client (Postman/Frontend)           │
└────────────────────┬────────────────────────────────────────┘
                     │ Request (with JWT token)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  NestJS Application                          │
│                  (/src)                                      │
└────────┬──────────────────────────────────────┬─────────────┘
         │                                      │
         ▼                                      ▼
    ┌─────────────┐                    ┌──────────────────┐
    │ Controller  │                    │ Other Modules    │
    │ (Routes)    │                    │ (Auth, Users)    │
    │             │                    └──────────────────┘
    │ 10 Routes   │
    └──────┬──────┘
           │
           ▼
    ┌──────────────────┐
    │ Service Layer    │
    │ (Business Logic) │
    │                  │
    │ 15+ Methods      │
    │                  │
    │ Validation       │
    │ Filtering        │
    │ Pagination       │
    └────────┬─────────┘
             │
             ▼
    ┌──────────────────────────┐
    │ Database Layer           │
    │ (Raw SQL)                │
    │                          │
    │ EmployeePrismaService    │
    │ (Dynamic connections)    │
    └────────┬─────────────────┘
             │
             ▼
    ┌──────────────────────────┐
    │ PostgreSQL Databases     │
    │                          │
    │ Master DB                │
    │ (hr_app_db)              │
    │ - Tenants metadata       │
    │ - Admin users            │
    │                          │
    │ Tenant DBs               │
    │ (tenant_{slug}_erp)      │
    │ - Employees (40+ fields) │
    │ - Users with full info   │
    └──────────────────────────┘
```

## 📊 Employee CRUD Flow

```
CREATE EMPLOYEE
┌─────────────────────────┐
│ POST /employees         │
│ (with JWT token)        │
└────────┬────────────────┘
         │ Request body:
         │ email, password, firstName, lastName, position, etc.
         ▼
┌─────────────────────────────────────┐
│ EmployeesController.createEmployee()│
│ - Validate input                    │
│ - Check JWT token                   │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│ EmployeesService.createEmployee()   │
│ - Validate email uniqueness         │
│ - Hash password                     │
│ - Start transaction                 │
└────────┬────────────────────────────┘
         │
         ├─────────────────────────────────┐
         │                                 │
         ▼                                 ▼
    INSERT User             INSERT Employee
    - email, password       - userId, firstName
    - firstName, lastName   - lastName, position
    - role: EMPLOYEE        - department, joinDate
    - isActive: true        - All optional fields
                            - managerId: null
                            - isActive: true
         │                                 │
         └─────────────────────────────────┘
                   │
                   ▼
         ┌──────────────────────┐
         │ COMMIT Transaction   │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ Return EmployeeDto   │
         │ with all fields      │
         └──────────────────────┘


LIST EMPLOYEES WITH FILTERS
┌────────────────────────────────────────────┐
│ GET /employees?page=1&limit=10&search=john │
│ &department=Engineering&isActive=true       │
│ &sortBy=firstName&sortOrder=asc             │
└────────┬───────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ EmployeesService.getEmployees()      │
│ - Parse filters                      │
│ - Build WHERE clause                 │
│ - Calculate OFFSET for pagination    │
│ - Build ORDER BY clause              │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ Execute SELECT query with:               │
│ - WHERE: search, department, status, etc │
│ - LIMIT 10 OFFSET 0                      │
│ - ORDER BY firstName ASC                 │
│ - Also get COUNT(*) for total            │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ Format Results                           │
│ - Convert BigInt to string               │
│ - Create EmployeeProfileDto objects      │
│ - Include pagination metadata            │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ Return PaginatedEmployeeResponseDto      │
│ {                                        │
│   data: [...],                           │
│   page: 1,                               │
│   limit: 10,                             │
│   total: 25,                             │
│   pages: 3                               │
│ }                                        │
└──────────────────────────────────────────┘
```

## 🌳 Organizational Hierarchy Flow

```
SET MANAGER
┌──────────────────────────────────────┐
│ PUT /employees/2/manager              │
│ { "managerId": 1 }                    │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ EmployeesService.setManager()         │
│ - Verify employee 2 exists            │
│ - Verify manager 1 exists             │
│ - Prevent self-assignment             │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ UPDATE employees                         │
│ SET managerId = 1, updatedAt = NOW()    │
│ WHERE id = 2                             │
└────────┬───────────────────────────────┘
         │
         ▼
│ Return updated employee with managerId=1 │


GET ORGANIZATION TREE
┌──────────────────────────────────────┐
│ GET /employees/1/organization-tree    │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ EmployeesService.getOrganizationTree()   │
│ - Get employee 1                         │
│ - Start recursive build                  │
└────────┬───────────────────────────────┘
         │
         ▼
┌───────────────────────────────────────┐
│ buildOrganizationTree(employee1)       │
│                                        │
│ 1. Fetch all subordinates              │
│    (WHERE managerId = 1)               │
│                                        │
│ 2. For each subordinate:               │
│    - Recursively build their tree      │
│    - Attach as subordinates array      │
│                                        │
│ 3. Return complete tree structure      │
└───────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Return TreeDto:                      │
│ {                                    │
│   id: 1, firstName: John,            │
│   subordinates: [                    │
│     {                                │
│       id: 2, firstName: Jane,        │
│       subordinates: [                │
│         {                            │
│           id: 3, firstName: Bob,     │
│           subordinates: []           │
│         }                            │
│       ]                              │
│     }                                │
│   ]                                  │
│ }                                    │
└──────────────────────────────────────┘
```

## 🔐 Authentication Flow

```
CLIENT → REQUEST
    │
    ├─ Authorization: Bearer <jwt_token>
    └─ Content-Type: application/json

         ▼
    ┌─────────────────────────┐
    │ JwtAuthGuard            │
    │ (Guards all routes)     │
    │                         │
    │ 1. Extract token        │
    │ 2. Verify signature     │
    │ 3. Validate expiry      │
    │ 4. Extract user info    │
    └────────┬────────────────┘
             │
        YES  │  NO
        ▼    └──→ 401 Unauthorized
    ┌─────────────────────────┐
    │ Token Valid             │
    │ User Authenticated      │
    └────────┬────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ Proceed to Controller   │
    │ & Service               │
    └────────┬────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ Multi-tenant Check      │
    │ (tenant_slug param)     │
    └────────┬────────────────┘
             │
        YES  │  NO
        ▼    └──→ 403 Forbidden
    ┌─────────────────────────┐
    │ Process Request         │
    │ with Tenant Isolation   │
    └─────────────────────────┘
```

## 📦 Data Flow: Create Employee

```
CLIENT REQUEST
    │
    ▼
┌──────────────────────────┐
│ HTTP POST Body           │
│ {                        │
│   email: "john@..."      │
│   firstName: "John"      │
│   lastName: "Doe"        │
│   position: "Developer"  │
│   department: "Engineering"
│   joinDate: "2024-01-15" │
│   ... 30+ optional fields│
│ }                        │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ CreateEmployeeDto (Validation)       │
│ - @IsEmail() on email                │
│ - @IsString() on strings             │
│ - @IsDateString() on dates           │
│ - @IsEnum() on enums                 │
│ - @IsOptional() on optional fields   │
└────────┬─────────────────────────────┘
         │ Valid ✓
         ▼
┌──────────────────────────────────────┐
│ Service Logic                        │
│ - Hash password                      │
│ - Check email exists                 │
│ - Check manager exists (if provided) │
└────────┬─────────────────────────────┘
         │ Valid ✓
         ▼
┌──────────────────────────────────────┐
│ Database Insertion                   │
│ BEGIN TRANSACTION                    │
│                                      │
│ 1. INSERT INTO users:                │
│    - email, password hash            │
│    - firstName, lastName             │
│    - All contact/personal info       │
│    - Returns: user.id (BigInt)       │
│                                      │
│ 2. INSERT INTO employees:            │
│    - userId: user.id                 │
│    - All fields                      │
│    - managerId: null/provided        │
│    - isActive: true                  │
│    - createdAt, updatedAt: NOW()     │
│    - Returns: employee record        │
│                                      │
│ COMMIT TRANSACTION                   │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Response Formatting                  │
│ - Convert BigInt to string           │
│ - Format dates to ISO 8601           │
│ - Create EmployeeProfileDto          │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ HTTP 201 Response                    │
│ {                                    │
│   id: "1",                           │
│   userId: "1",                       │
│   email: "john@...",                 │
│   firstName: "John",                 │
│   lastName: "Doe",                   │
│   position: "Developer",             │
│   department: "Engineering",         │
│   joinDate: "2024-01-15T00:00Z",     │
│   ...all other fields...,            │
│   createdAt: "2024-12-14T12:15Z",    │
│   updatedAt: "2024-12-14T12:15Z"     │
│ }                                    │
└──────────────────────────────────────┘
```

## 🗄️ Database Schema Relationship

```
┌──────────────────────────────────────────────────────────┐
│                   MASTER DATABASE                        │
│                   (hr_app_db)                            │
│                                                          │
│  ┌──────────────┐         ┌──────────────┐              │
│  │  tenants     │────────→│ users        │              │
│  │              │  1:N    │ (Admin Users)│              │
│  │ - id (CUID)  │         │              │              │
│  │ - name       │         │ - id (CUID)  │              │
│  │ - slug       │         │ - email      │              │
│  │ - createdAt  │         │ - role       │              │
│  └──────────────┘         │ - createdAt  │              │
│                           └──────────────┘              │
│                                                          │
└──────────────────────────────────────────────────────────┘

FOR EACH TENANT, SEPARATE DATABASE IS CREATED

┌──────────────────────────────────────────────────────────┐
│        TENANT DATABASE 1: {slug1}_erp                    │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ users                                              │ │
│  │ - id (BigInt)                                      │ │
│  │ - email                                            │ │
│  │ - firstName, lastName                             │ │
│  │ - role (ADMIN, HR, MANAGER, EMPLOYEE)            │ │
│  │ - phoneNumber, address, city, province           │ │
│  │ - dateOfBirth, gender, maritalStatus            │ │
│  │ - emergencyContact*, bank*                       │ │
│  │ - isActive, createdAt, updatedAt                 │ │
│  └────────────────────────┬───────────────────────────┘ │
│                           │ 1:1 (userId)               │
│  ┌────────────────────────▼───────────────────────────┐ │
│  │ employees                                          │ │
│  │ - id (BigInt)                                      │ │
│  │ - userId (unique FK)                              │ │
│  │ - managerId (self-referential FK)                │ │
│  │ - firstName, lastName, position, department      │ │
│  │ - joinDate                                        │ │
│  │ - employeeNumber, dateOfBirth, gender            │ │
│  │ - nationality, religion, bloodType               │ │
│  │ - idNumber, taxNumber (unique)                   │ │
│  │ - phoneNumber*, address*, city*                  │ │
│  │ - emergencyContact*, bank*, employment*          │ │
│  │ - profilePicture, baseSalary                      │ │
│  │ - isActive, deletedAt (soft delete)              │ │
│  │ - createdAt, updatedAt                           │ │
│  │                                                   │ │
│  │ INDEXES:                                         │ │
│  │ - firstName, lastName (search)                   │ │
│  │ - position, department (filtering)               │ │
│  │ - managerId (hierarchy)                          │ │
│  │ - isActive, deletedAt (status)                   │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│        TENANT DATABASE 2: {slug2}_erp                    │
│        (Identical structure, separate data)              │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Filtering & Pagination Logic

```
USER REQUEST
    │
    ├─ ?page=2&limit=20
    ├─ &search=john
    ├─ &department=Engineering
    ├─ &employmentStatus=PERMANENT
    ├─ &isActive=true
    ├─ &managerId=1
    ├─ &sortBy=firstName
    └─ &sortOrder=asc
         │
         ▼
┌────────────────────────────────────────────┐
│ Parse Query Parameters                     │
│ - page = 2, limit = 20                    │
│ - offset = (2-1) * 20 = 20                │
│ - Validate limit: 1-100, default 10       │
└────────┬───────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────┐
│ Build WHERE Clause                        │
│                                           │
│ WHERE "deletedAt" IS NULL                │
│   AND ("firstName" ILIKE '%john%'        │
│        OR "lastName" ILIKE '%john%'      │
│        OR "position" ILIKE '%john%'      │
│        OR "department" ILIKE '%john%')   │
│   AND "department" = 'Engineering'       │
│   AND "employmentStatus" = 'PERMANENT'   │
│   AND "isActive" = true                  │
│   AND "managerId" = 1                    │
└────────┬───────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────┐
│ Build Query                               │
│                                           │
│ SELECT COUNT(*) FROM employees           │
│ WHERE ... (get total)                    │
│                                           │
│ SELECT * FROM employees                 │
│ WHERE ...                                │
│ ORDER BY "firstName" ASC                │
│ LIMIT 20 OFFSET 20                      │
└────────┬───────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────┐
│ Execute & Format Results                 │
│                                           │
│ total = 45 (from COUNT)                 │
│ employees = [... 20 records ...]         │
│ pages = ceil(45 / 20) = 3               │
│                                           │
│ Response: {                               │
│   data: [...],                           │
│   page: 2,                               │
│   limit: 20,                             │
│   total: 45,                             │
│   pages: 3                               │
│ }                                        │
└────────────────────────────────────────────┘
```

## ✨ Summary

The system is designed with:

1. **Multi-layer architecture** - Controller → Service → Database
2. **Data isolation** - Master DB + separate tenant DBs
3. **Type safety** - Full TypeScript throughout
4. **Efficient queries** - Indexes, pagination, filtering
5. **Audit trail** - Soft delete with timestamps
6. **Hierarchy support** - Self-referential relationships
7. **Security** - JWT auth, input validation
8. **Performance** - Optimized SQL, caching-ready

